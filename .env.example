# BioPredict Environment Variables Template
# Copy this file to .env and fill in your actual values
# Note: In Replit, use Secrets instead of .env for better security

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@host/database

# Individual PostgreSQL Credentials (auto-populated by Replit)
PGHOST=your-host.neon.tech
PGPORT=5432
PGUSER=your-username
PGPASSWORD=your-password
PGDATABASE=neondb

# Authentication
JWT_SECRET=your-secure-random-secret-key-here

# OpenAI/OpenRouter API Key for AI Chat
OPENAI_API_KEY=your-openai-or-openrouter-api-key

# Node Environment
NODE_ENV=development
